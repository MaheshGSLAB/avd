name: "Collection code testing"

"on": pull_request

concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref }}
  cancel-in-progress: true

env:
  # Set -vvv is ACTIONS_STEP_DEBUG is set
  # Apparently it is set in secrets when running with debug
  ANSIBLE_VERBOSITY: ${{ secrets.ACTIONS_STEP_DEBUG && 3 || 0 }}
  AVD_NEVER_RUN_FROM_SOURCE: 1
  PY_COLORS: 1 # allows molecule colors to be passed to GitHub Actions
  ANSIBLE_FORCE_COLOR: 1 # allows ansible colors to be passed to GitHub Actions

jobs:
  file-changes:
    runs-on: ubuntu-latest
    outputs:
      eos_design: ${{ steps.filter.outputs.eos_design }}
      config_gen: ${{ steps.filter.outputs.config_gen }}
      cloudvision: ${{ steps.filter.outputs.cloudvision }}
      dhcp: ${{ steps.filter.outputs.dhcp }}
      plugins: ${{ steps.filter.outputs.plugins }}
      requirements: ${{ steps.filter.outputs.requirements }}
      docs: ${{ steps.filter.outputs.docs }}
    steps:
      - uses: actions/checkout@v4
      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            eos_design:
              - 'ansible_collections/arista/avd/roles/eos_designs/*'
              - 'ansible_collections/arista/avd/roles/eos_designs/**/*'
              - '.github/workflows/pull-request-management.yml'
              - 'ansible_collections/arista/avd/molecule/*'
              - 'ansible_collections/arista/avd/molecule/**/*'
              - 'python-avd/pyavd/_eos_designs/*'
              - 'python-avd/pyavd/_eos_designs/**/*'
              - '!ansible_collections/arista/avd/roles/eos_config_deploy_cvp/*'
              - '!ansible_collections/arista/avd/roles/eos_config_deploy_cvp/**/*'
              - '!ansible_collections/arista/avd/molecule/cvp_configlet_upload/*'
              - '!ansible_collections/arista/avd/molecule/cvp_configlet_upload/**/*'
            config_gen:
              - 'ansible_collections/arista/avd/roles/eos_cli_config_gen/*'
              - 'ansible_collections/arista/avd/roles/eos_cli_config_gen/**/*'
              - '.github/workflows/pull-request-management.yml'
              - 'ansible_collections/arista/avd/molecule/eos_cli_config_gen*/*'
              - 'ansible_collections/arista/avd/molecule/eos_cli_config_gen*/**/*'
              - 'python-avd/pyavd/_eos_cli_config_gen/*'
              - 'python-avd/pyavd/_eos_cli_config_gen/**/*'
            validate_state:
              - 'ansible_collections/arista/avd/roles/eos_validate_state/*'
              - 'ansible_collections/arista/avd/roles/eos_validate_state/**/*'
            cloudvision:
              - 'ansible_collections/arista/avd/roles/eos_config_deploy_cvp/*'
              - 'ansible_collections/arista/avd/roles/eos_config_deploy_cvp/**/*'
              - 'ansible_collections/arista/avd/molecule/cvp_configlet_upload/*'
              - 'ansible_collections/arista/avd/molecule/cvp_configlet_upload/**/*'
              - '.github/workflows/pull-request-management.yml'
            dhcp:
              - 'ansible_collections/arista/avd/roles/dhcp_provisioner/*'
              - 'ansible_collections/arista/avd/roles/dhcp_provisioner/**/*'
              - '.github/workflows/pull-request-management.yml'
            plugins:
              - 'ansible_collections/arista/avd/plugins/filter/**'
              - 'ansible_collections/arista/avd/plugins/test/**'
            requirements:
              - 'ansible_collections/arista/avd/requirements.txt'
              - 'ansible_collections/arista/avd/requirements-dev.txt'
              - 'ansible_collections/arista/avd/meta/runtime.yml'
              - '.github/requirements-ci.txt'
              - '.github/workflows/pull-request-management.yml'
            docs:
              - '.github/workflows/pull-request-management.yml'
              - 'mkdocs.yml'
              - 'ansible_collections/arista/avd/docs/**'
              - 'ansible_collections/arista/avd/roles/**/*.md'
              - 'ansible_collections/arista/avd/**/*.md'
              - 'ansible_collections/arista/avd/README.md'
              - 'ansible_collections/arista/avd/**/*.schema.yml'
            pyavd:
              - 'python_avd/*'
              - 'python_avd/**/*'
  create-avd-slim2:
    runs-on: ubuntu-latest
    container:
      image: ubuntu:latest
      options: --cpus 1
    steps:
      - name: Check out repository code
        uses: actions/checkout@v4
      - name: Set up Docker
        run: |
          apt-get update
          apt-get install -y docker.io

      - name: Convert GitHub username to lowercase
        id: lowercase
        run: |
          echo "::set-output name=lowercase::$(echo '${{ github.repository_owner }}' | tr '[:upper:]' '[:lower:]')"

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ steps.lowercase.outputs.lowercase }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      
      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          file: ansible-avd/.github/Dockerfile
          push: true
          tags: avd-slim2:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max
  # create-avd-slim:
  #   runs-on: ubuntu-latest
  #   container:
  #     image: ubuntu:latest
  #     options: --cpus 1
  #   steps:
  #     - name: Check out repository code
  #       uses: actions/checkout@v4
  #     - name: Set up Docker
  #       run: |
  #         apt-get update
  #         apt-get install -y docker.io

  #     - name: Convert GitHub username to lowercase
  #       id: lowercase
  #       run: |
  #         echo "::set-output name=lowercase::$(echo '${{ github.repository_owner }}' | tr '[:upper:]' '[:lower:]')"

  #     - name: Log in to GitHub Container Registry
  #       uses: docker/login-action@v2
  #       with:
  #         registry: ghcr.io
  #         username: ${{ steps.lowercase.outputs.lowercase }}
  #         password: ${{ secrets.GITHUB_TOKEN }}

  #     - name: Create Dockerfile
  #       run: |
  #         cat <<EOF > Dockerfile
  #         FROM ubuntu:latest
  #         RUN apt-get update && apt-get install -y --no-install-recommends \\
  #             openssh-client git make python3-pip python3-dev docker.io \\
  #             && pip install uv --break-system-packages \\
  #             && apt-get clean \\
  #             && rm -rf /var/lib/apt/lists/*

  #         # Copy repository code into the image
  #         COPY . /avd

  #         # Install UV and Python virtual environments
  #         RUN uv python install 3.10 3.11 3.12 3.13 && \\
  #             uv venv --python 3.10 /UV3.10 && /UV3.10/bin/python -m ensurepip --upgrade && \\
  #             uv venv --python 3.11 /UV3.11 && /UV3.11/bin/python -m ensurepip --upgrade && \\
  #             uv venv --python 3.12 /UV3.12 && /UV3.12/bin/python -m ensurepip --upgrade && \\
  #             uv venv --python 3.13 /UV3.13 && /UV3.13/bin/python -m ensurepip --upgrade && \\
  #             /UV3.10/bin/python -m pip install -r /avd/ansible_collections/arista/avd/requirements-dev.txt --upgrade && \\
  #             /UV3.11/bin/python -m pip install -r /avd/ansible_collections/arista/avd/requirements-dev.txt --upgrade && \\
  #             /UV3.12/bin/python -m pip install -r /avd/ansible_collections/arista/avd/requirements-dev.txt --upgrade && \\
  #             /UV3.13/bin/python -m pip install -r /avd/ansible_collections/arista/avd/requirements-dev.txt --upgrade

  #         EOF

  #     - name: Build and Push Docker Image
  #       id: build-image
  #       run: |
  #         IMAGE_TAG=ghcr.io/${{ steps.lowercase.outputs.lowercase }}/avd-slim-test:0.1
  #         docker build -t $IMAGE_TAG .
  #         docker push $IMAGE_TAG


  # setup-avd:
  #   name: Install AVD Requirements and Build Image
  #   needs: [ file-changes ]
  #   runs-on: ubuntu-latest
  #   container:
  #     image: ghcr.io/maheshgslab/avd-slim-test:0.1
  #     options: --cpus 1

  #   outputs:
  #     image_tag: ${{ steps.build-image.outputs.image_tag }}

  #   steps:
  #     - name: Check out repository code
  #       uses: actions/checkout@v4

  #     - name: Convert GitHub username to lowercase
  #       id: lowercase
  #       run: |
  #         echo "::set-output name=lowercase::$(echo '${{ github.repository_owner }}' | tr '[:upper:]' '[:lower:]')"

  #     - name: Log in to GitHub Container Registry
  #       uses: docker/login-action@v2
  #       with:
  #         registry: ghcr.io
  #         username: ${{ steps.lowercase.outputs.lowercase }}
  #         password: ${{ secrets.GITHUB_TOKEN }}
  #     - name: Set up Docker Buildx
  #       uses: docker/setup-buildx-action@v3

  #     - name: Build and push
  #       id: build-and-push
  #       uses: docker/build-push-action@v6
  #       with:
  #         context: ../
  #         file: ../Dockerfile  # Path to your Dockerfile
  #         push: true
  #         tags: ghcr.io/maheshgslab/avd-slim-test:10-${{ github.run_number }}-${{ github.sha }}
  #         cache-from: type=registry,ref=ghcr.io/maheshgslab/avd-slim-test:0.1
  #         cache-to: type=inline

  #     - name: Extract image tag
  #       id: image-tag
  #       run: |
  #         echo "image_tag=ghcr.io/maheshgslab/avd-slim-test:10-${{ github.run_number }}-${{ github.sha }}" >> $GITHUB_ENV
      # - name: Install Requirements and Build Image
      #   id: build-image
      #   run: |
      #     # Install Python requirements in the container
      #     PYTHON_VERSIONS="UV3.10 UV3.11 UV3.12 UV3.13"
      #     REQUIREMENTS_FILE="ansible_collections/arista/avd/requirements-dev.txt"
      #     PACKAGES="mock pytest pytest-mock pytest-xdist pyyaml"

      #     # Loop through the Python versions and install requirements
      #     for PYTHON_VERSION in $PYTHON_VERSIONS; do
      #       /$PYTHON_VERSION/bin/python -m pip install $PACKAGES "ansible-core<2.19.0" -r $REQUIREMENTS_FILE --upgrade
      #     done

      #     # Create a new image from the running container
      #     SHORT_SHA=$(echo "9f89f9bbc6f1a222870b9ad45c13fdce3a4ee1b0" | cut -c1-8)
      #     IMAGE_TAG="ghcr.io/maheshgslab/avd-slim-test:10-$SHORT_SHA"

      #     # Commit the running container to create a new image
      #     CONTAINER_ID=$(docker ps -q --filter ancestor=ghcr.io/maheshgslab/avd-slim-test:0.1)
      #     docker commit $CONTAINER_ID $IMAGE_TAG

      #     # Push the newly created image to GitHub Container Registry
      #     docker push $IMAGE_TAG

      #     # Set output for the image tag
      #     echo "::set-output name=image_tag::$IMAGE_TAG"

  # build-pyavd:
  #   name: Install pyavd and Build Image
  #   needs: setup-avd
  #   runs-on: ubuntu-latest
  #   container:
  #     image: ${{ needs.setup-avd.outputs.image_tag }}
  #     options: --cpus 1
  #   outputs:
  #     image_tag: ${{ steps.wheel-image-tag.outputs.wheel_image_tag }}
  #   steps:
  #     - name: Check out repository code
  #       uses: actions/checkout@v4
  #     - name: Convert GitHub username to lowercase
  #       id: lowercase
  #       run: |
  #         echo "::set-output name=lowercase::$(echo '${{ github.repository_owner }}' | tr '[:upper:]' '[:lower:]')"

  #     - name: Log in to GitHub Container Registry
  #       uses: docker/login-action@v2
  #       with:
  #         registry: ghcr.io
  #         username: ${{ steps.lowercase.outputs.lowercase }}
  #         password: ${{ secrets.GITHUB_TOKEN }}

  #     - name: Additional Task in Second Container
  #       id: wheel-image-tag
  #       run: |
  #         cd python-avd
  #         python3 -m pip install --break-system-packages build
  #         python3 -m build --wheel --outdir /tmp/wheels

  #         # Install the generated wheel for all required Python versions
  #         for VERSION in UV3.10 UV3.11 UV3.12 UV3.13; do
  #           /$VERSION/bin/python -m pip install /tmp/wheels/*.whl
  #         done
  #         cd ..
  #         cp -rT ./ /avd
  #         # Generate a unique tag using run number and truncated commit SHA
  #         WHEEL_IMAGE_TAG=${{ needs.setup-avd.outputs.image_tag }}-2

  #         # Build the updated image from the current container state
  #         docker commit $(docker ps -q --filter ancestor=${{ needs.setup-avd.outputs.image_tag }}) $WHEEL_IMAGE_TAG

  #         # Push the updated image to GitHub Container Registry
  #         docker push $WHEEL_IMAGE_TAG
  #         echo "::set-output name=wheel_image_tag::$WHEEL_IMAGE_TAG"

  # run-ansible-tests:
  #   name: Run Ansible
  #   needs: build-pyavd
  #   runs-on: ubuntu-latest
  #   container:
  #     image: ${{ needs.build-pyavd.outputs.image_tag }}
  #     options: --cpus 1

  #   strategy:
  #     matrix:
  #       task:
  #         - name: Unit Tests 3.12
  #           command: ". /UV3.12/bin/activate && cd /avd/ansible_collections/arista/avd && ansible-test units -vv"
  #         - name: Integration Tests 3.12
  #           command: ". /UV3.12/bin/activate && cd /avd/ansible_collections/arista/avd && ansible-test integration -vv"
  #         - name: Integration Tests 3.13
  #           command: ". /UV3.13/bin/activate && cd /avd/ansible_collections/arista/avd && ansible-test integration -vv"
  #         - name: Sanity Tests 3.13
  #           command: ". /UV3.13/bin/activate && cd /avd/ansible_collections/arista/avd && ansible-test sanity --color yes -v"
  #         - name: Sanity Tests 3.12
  #           command: ". /UV3.12/bin/activate && cd /avd/ansible_collections/arista/avd && ansible-test sanity --color yes -v"
  #         - name: Lint Tests 3.11
  #           command: ". /UV3.11/bin/activate && ansible-galaxy collection install -r /avd/ansible_collections/arista/avd/collections.yml && cd /avd/ansible_collections/arista/avd && ansible-lint --force-color --strict -v"
  #   steps:
  #     - name: Convert GitHub username to lowercase
  #       id: lowercase
  #       run: |
  #         echo "::set-output name=lowercase::$(echo '${{ github.repository_owner }}' | tr '[:upper:]' '[:lower:]')"

  #     - name: Log in to GitHub Container Registry
  #       uses: docker/login-action@v2
  #       with:
  #         registry: ghcr.io
  #         username: ${{ steps.lowercase.outputs.lowercase }}
  #         password: ${{ secrets.GITHUB_TOKEN }}
  #     - name: Run Task - ${{ matrix.task.name }}
  #       run: |
  #         git config --global --add safe.directory /__w/avd/avd
  #         ${{ matrix.task.command }}

  # galaxy_importer:
  #   name: Test galaxy-importer
  #   runs-on: ubuntu-20.04 # Older version to be compatible with old python
  #   env:
  #     GALAXY_IMPORTER_CONFIG: galaxy-importer/galaxy-importer.cfg
  #   container:
  #     image: ${{ needs.build-pyavd.outputs.image_tag }}
  #     options: --cpus 1
  #   steps:
  #     - name: 'Install Ansible requirements'
  #       run: |
  #         . /UV3.10/bin/activate && ansible-galaxy collection install -r /avd/ansible_collections/arista/avd/collections.yml
  #     - name: Install galaxy-importer
  #       # Install the specific version of galaxy-importer used on galaxy.ansible.com
  #       # The version conflicts with our requirements,
  #       # so we let the galaxy-importer version resolve remaining requirements.
  #       run: |
  #         /UV3.10/bin/python -m pip install "galaxy-importer==0.4.26"
  #     - name: 'Build ansible package'
  #       run: . /UV3.10/bin/activate && make collection-build
  #     - name: 'Run galaxy-importer checks'
  #       run: /UV3.10/bin/python -m galaxy_importer.main *.tar.gz
  #     - uses: actions/upload-artifact@v4
  #       with:
  #         name: importer-logs
  #         path: ./importer_result.json

  # cleanup-ghcr-images:
  #   name: Cleanup GHCR Images
  #   runs-on: ubuntu-latest
  #   #needs: run-ansible-tests
  #   steps:
  #     - name: Log in to GitHub Container Registry
  #       run: |
  #         echo "${{ secrets.GITHUB_TOKEN }}" | gh auth login --with-token
  #     - name: Delete Container Job 1 Image
  #       run: |
  #         IMAGE_TAG="ghcr.io/maheshgslab/avd-slim-test:3-11c8ad29-2"
  #         IMAGE_REPO=$(echo "$IMAGE_TAG" | sed 's|ghcr.io/||' | cut -d':' -f1)
  #         TAG=$(echo "$IMAGE_TAG" | cut -d':' -f2)

  #         echo "Fetching version ID for tag: $TAG in repository: $IMAGE_REPO"

  #         # Request to fetch the versions for the container
  #         response=$(gh api "repos/${IMAGE_REPO}/packages/container/avd-slim-test/versions" || echo "Error fetching API response")

  #         # Log the full API response for debugging
  #         echo "API Response: $response"

  #         # Extract the version ID using jq
  #         VERSION_ID=$(echo "$response" | jq -r ".[] | select(.metadata.container.tags[] | contains(\"$TAG\")) | .id")

  #         if [ -n "$VERSION_ID" ]; then
  #           echo "Deleting version ID $VERSION_ID for tag $TAG"
  #           gh api -X DELETE "repos/${IMAGE_REPO}/packages/container/avd-slim-test/versions/$VERSION_ID" \
  #             && echo "Successfully deleted tag $TAG" \
  #             || echo "Failed to delete tag $TAG"
  #         else
  #           echo "Version ID for tag $TAG not found. Skipping deletion."
  #         fi
